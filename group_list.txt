{"step_index":1927,"source":"USER_EXPLICIT","type":"USER_INPUT","status":"DONE","created_at":"2026-09-01T19:46:12Z","content":"<USER_REQUEST>\nB2B Supply & Procurement Platform — Enhancement Backlog\n\nContext: This is a consolidated list of every enhancement and bug fix agreed on, building on top of the completed Phases 1-10 platform. Work through these as separate groups, in the order listed (later groups depend on earlier ones — e.g. Group 3's roles are used by Group 9). Give me a plan for each group separately before building it, same as every other phase in this project. Do not touch existing checkout, credit-check, or stock-locking logic except where a group explicitly calls for it.\n\nGroup 1: Critical Bug Fixes (do first, standalone)\nQuote acceptance 500 error on expired quotes: in quote.service.ts acceptQuote(), the expired-quote branch calls await t.commit() and then throw new BadRequestException(...). The outer catch block unconditionally calls await t.rollback() on any thrown error — but the transaction was already committed, so this throws a second, unrelated error that surfaces as an unhandled 500, masking the intended 400 message. Fix: handle the expired-quote case by returning/throwing directly without routing through the catch block's rollback (e.g. return early before the try/catch's rollback path can run, or restructure so committed-then-thrown never reaches an unconditional rollback).\nMissing CR & Tax Registration on invoice PDF: the PDF template only prints client.company_name in the Bill To section — no commercial registration or tax registration lines exist anywhere in the document. Add both as separate lines (e.g. \"Commercial Registration: {value}\", \"Tax Registration: {value}\") to the Bill To block in pdf.service.ts, pulling from the Client's existing commercial_registration and tax_registration fields. If either is null on the client, omit that line gracefully rather than printing \"null\" or an empty label.\nGroup 2: Invoice — Split \"View\" and \"PDF\" into Two Actions\n\nReplace the single \"View Invoice\" button (dashboard and storefront) with two separate actions:\n\n\"View\": opens an in-app Invoice Detail view (modal or page) rendering invoice number, date, sales order reference, payment method, bill-to (client name, CR, tax registration), line items, subtotal/tax/grand total, and payment status — straight from the database, no PDF generation involved.\n\"PDF\": keeps the existing smart-regeneration logic exactly as built (regenerate only if pdf_url is null or updatedAt is newer than pdf_generated_at, overwriting the same file — no version history needed), then opens the resulting PDF. Build the Invoice Detail view as one reusable component, and use it in both the dashboard (Finance) and storefront (client's own invoices).\nGroup 3: New Roles — content and operator\nAdd content role: full Products CRUD (create/edit/deactivate/stock is unchanged for warehouse). Remove product-edit access from sales — content becomes the primary Products role.\nAdd operator role: view/edit order details, create order-revision quotes (see Group 9). Additive — does not remove sales' order-approve right or warehouse's process/ship/deliver rights.\nUpdate @Roles() decorators on the relevant Products and Orders endpoints accordingly. Keep the existing enum-based RolesGuard pattern — no permissions/checkbox system.\nGroup 4: Client Name Display Fix\n\nShow the client's company name (not just client_id) on: the Orders list, Order detail view, Quotes list, and Quote detail view — in both the dashboard and anywhere else client_id is currently shown bare to a staff user.\n\nGroup 5: Order & Invoice Discount\nAdd discount_amount (decimal, default 0) to the Order model via migration — an absolute currency value, not a percentage.\ntotal_amount should reflect the order's total net of discount.\nWhenever an order's discount changes (creation or later edit), the linked Invoice's subtotal/tax_amount/grand_total must be recalculated and the invoice row updated (so its updatedAt bumps and the Group 2 \"PDF\" smart-regeneration picks up the change) — don't let the invoice silently go stale relative to the order.\nAdd a discount input to wherever order creation/editing happens for operator/sales/super_admin.\nGroup 6: Favorite / Priority Clients\nAdd is_priority (boolean, default false) to the Client model via migration.\nToggle-able by sales/super_admin from the Clients page or client detail view.\nShow as a visible badge/marker in the Clients list.\nGroup 7: Low Stock Threshold\nAdd low_stock_threshold (integer, nullable) to the Product model via migration. Default it to 20% of the product's stock_level at creation time, but make it freely editable afterward — do not tie it to a percentage of an ever-changing \"original stock\" number.\nIn the dashboard Products list (and storefront catalog, if desired), highlight/badge a product in orange or red with a \"Low Stock\" label when stock_level <= low_stock_threshold.\nGroup 8: Product Categories as a Real Entity\nNew Category model (id, name, unique).\nAdd category_id to Product, migrating existing free-text category string values into real Category rows (dedupe by name).\nSimple category management (create/list) restricted to content/super_admin — minimal, doesn't need its own dedicated page if a lightweight inline \"add new category\" option in the product form is simpler.\nReplace the free-text category field in the dashboard's Create/Edit Product form with a dropdown populated from Category.\nGroup 9: Order Revisions via Quotes (Order Modification & Customer Approval)\nAdd optional related_order_id (nullable, belongsTo Order) to the Quote model.\noperator (and existing sales/super_admin) can create a quote with related_order_id set, proposing changes (items/quantities/discount) to an existing order.\nClient sees it in their existing Quotes page/list — label it distinctly (e.g. \"Order Update\" vs. \"New Quote\") based on whether related_order_id is set, so it's not confused with a fresh quote.\nAccept logic branches: if related_order_id is null, behave exactly as today (create new Order + Invoice). If related_order_id is set, update the existing order's items/total_amount instead of creating a new order, and recalculate/touch the existing linked invoice (same pattern as Group 5) rather than creating a second invoice/sequence number.\nReject logic: existing order is left unchanged; the revision quote is marked rejected.\nDefault the admin Quotes list/reporting to related_order_id IS NULL (i.e. show new-business quotes by default), with a filter option to include order-revision quotes — so Sales pipeline metrics aren't polluted by revisions.\nGroup 10: Order Activity Log + Status Duration Tracking\nNew OrderActivityLog model: order_id, action_type (e.g. created, status_changed, discount_changed, revision_proposed, revision_accepted, revision_rejected), actor (admin user id + name, or 'customer' if client-initiated), description, created_at.\nLog an entry on: order creation, every status transition (approve/process/ship/deliver/cancel), discount changes (Group 5), and every step of the order-revision flow (Group 9).\nDashboard: add an Activity Log section/tab on the Order Detail view, shown chronologically.\nDerive status-duration KPIs from this log rather than a separate duration table — compute time between consecutive status_changed entries per order. Build a simple aggregate endpoint (e.g. average time in each phase: created→approved, approved→processing, processing→shipped, shipped→delivered) for later use in Analytics. Don't maintain a second, separate source of truth for durations.\nGroup 11: Incidents Module\nNew Incident model: order_id (belongsTo Order), type (enum: damaged_product, damaged_order, missing_product, incorrect_product, other), description, created_by (admin user), status (enum: open, in_progress, resolved), resolution (text, nullable), timestamps.\nwarehouse (and super_admin) can create and update incidents tied to an order.\nNew dashboard section/page listing incidents, filterable by status/type, with a detail view to update status/resolution.\nGroup 12: Filtering & Sorting\n\nAdd date-range and client filters to the admin list endpoints for Clients, Products, Quotes, Orders, Invoices, and Incidents (in addition to any status/category filters already present per section). Add matching filter controls to each corresponding dashboard list page.\n\nGroup 13: Client Detail Expansion, History Export, Reorder Reminder\nExpand the Client Detail view to show: full contact info, commercial registration, tax registration, uploaded documents (needs a document upload capability similar to the existing product image upload — store via the same upload pattern), and that client's order/quote/invoice history in one place.\nAdd an \"Export History\" button on the client detail view that exports that client's orders, quotes, and invoices to an Excel file, reusing the existing exportToExcel utility.\nReorder reminder — flag this back to me before building the send mechanism: computing \"25+ days since last order\" and showing it as a badge/indicator in the Clients list is straightforward with existing data. But actually sending a reminder (manually or automatically) requires an outbound email capability that does not exist anywhere in this project yet. Don't guess at an email provider — implement the \"days since last order\" indicator and a manual \"Send Reminder\" button that's disabled/flagged as pending until I confirm which email service to integrate (e.g. SMTP via nodemailer, or a transactional provider). Ask me before choosing.\nGroup 14: Company Registration Requirements\nMake phone/contact number a required field (not optional) on client registration, if it isn't already enforced in the DTO.\nConfirm commercial_registration and tax_registration are required fields at registration, not optional.\nInstructions\nWork through the groups in order — later groups depend on earlier ones (Group 9 needs Group 3's operator role; Group 10 should ideally land before/alongside Group 9 so revisions get logged from the start).\nPlan each group separately and wait for my approval before building it, exactly as we've done for every phase so far.\nFlag any ambiguity rather than guessing, especially Group 13's email-sending requirement.\nOnce Groups 1-12 are done, the previously-stubbed Analytics dashboard page (from Phase 9) finally has real data to build against via Group 10's duration endpoints — treat that as a natural follow-up phase after this backlog, not part of it.\n\n\n</USER_REQUEST>\n<ADDITIONAL_METADATA>\nThe current local time is: 2026-09-01T22:46:12+03:00.\n</ADDITIONAL_METADATA>"}
